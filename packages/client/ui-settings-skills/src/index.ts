/** Host loader entry for the Skills settings browser implementation exported from `./client`. */

/** Host plugin body — no host-side behavior for the Skills settings section. */
export function apply(): void {}
