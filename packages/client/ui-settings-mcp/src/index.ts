/** Host loader entry for the MCP settings browser implementation exported from `./client`. */

/** Host plugin body — no host-side behavior for the MCP settings section. */
export function apply(): void {}
